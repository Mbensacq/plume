# Plume — dictée vocale locale — dépendances
# Python 3.10+ (testé sur 3.13.13, Windows 11). Voir README.md.
#
#   python -m venv .venv
#   .venv\Scripts\python -m pip install -r requirements.txt

# --- Cœur ---
faster-whisper==1.2.1      # moteur Whisper ; tire ctranslate2, onnxruntime, tokenizers, av...
soundcard==0.4.6           # capture audio : micros ET sorties (loopback WASAPI, Windows)
numpy==2.4.6               # traitement / mixage des buffers audio

# --- Accélération GPU : CUDA 12 + cuDNN 9 pour CTranslate2 ---
# Nécessaires pour device="cuda". Volumineux (~1,3 Go). Sans eux, ou en cas
# d'échec de chargement, l'application bascule automatiquement sur le CPU (int8).
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.23.0.39
